# RADPS
Radio Astronomy Data Processing System

##

To run the demo pipeline in a python environment with the dependencies installed and a prefect server hosted locally:
`python prefect_workflow/pipeline.py`