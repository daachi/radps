# RADPS
Radio Astronomy Data Processing System

##

To run the demo pipeline in a python environment with the dependencies installed, it is required to have a prefect server running:

`prefect server start &`

and then the pipeline can be executed using:

`python prefect_workflow/pipeline.py`
